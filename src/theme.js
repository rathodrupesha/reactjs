import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#FF6C23",
    },
    secondary: {
      main: "#333252",
    },
    otherColor: {
      main: "#999",
      blue: "#333252",
      lightBlack: "#18191F",
    },
    shape: {
      borderRadius: "24px",
    },
  },
});
